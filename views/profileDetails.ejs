<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 relative">
    
<a href="/" class="text-blue-200 absolute right-3 top-4">Home</a>
    <div class="lg:w-3/5 p-5 text-white  border-2 border-zinc-800 rounded-md mx-auto container h-screen bg-zinc-900 ">
           <div class="flex overflow-hidden gap-6 h-36 ">
            <img src="<%= data.image_url %>" class="h-full object-cover object-top  w-36 rounded-full " alt="user">
            <div class=" h-full flex flex-col item-center justify-center  ">
                <div >
                    <h1 class="text-white text-2xl font-thin "><%= data.username %></h1>

                </div>
                <div >
                    <h1 class="text-sm font-semibold mt-2"><%= data.name %></h1>
                    <p class=" leading-tight  text-sm font-thin"><%= data.description %></p>
                </div>
               
                
            </div>
           </div>  
           <hr class="w-full bg-zinc-400 h-[1px] my-3">
           <div class="w-full mt-4" >
            <h1 class="font-semibold text-2xl">Posts : </h1>
            <div class="mt-3 w-full h-full gap-4 flex flex-wrap">
                <% if(data1.length === 0){ %>
                   <h1>No Posts Yet !</h1>
               <% } else{ %>
                     
               <% data1.forEach(val=>{ %>
                <a href="/postDetail/<%= val._id %>">
                        <div class=" card h-42 w-36 flex flex-col items-center justify-center bg-zinc-800 overflow-hidden rounded-md p-2 ">
                            <img src="<%= val.image_url %>" class="h-36 w-36 object-cover object-top  rounded-md bg-blue-300 object-cover "  alt="">
                            <!-- <h1 class="leading-tight  text-sm mt-3 mb-2 font-thin w-full"><%= val.postDescription %></h1> -->
                           
                        </div>
                        </a>

                    <% }); %>
              <% } %>
                 
            </div>
           </div>
    </div>
   
</body>
</html>